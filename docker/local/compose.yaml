# Generated by erm docker sync. Do not edit manually.
services:
  postgres:
    container_name: "app-postgres"
    image: "postgres:18"
    # Use a custom image to bundle additional extensions.
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: "app"
      POSTGRES_USER: "user"
      POSTGRES_PASSWORD: "pass"
    volumes:
      - "app-data:/var/lib/postgresql/data"
      - "./initdb.d:/docker-entrypoint-initdb.d"
    healthcheck:
      test:
        - "CMD-SHELL"
        - "pg_isready -d app -U user"
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 5s
volumes:
  app-data:
    driver: local

